<% precentage = session[:score].to_f / session[:total_steps].to_f * 100 %>
<div class="row justify-content-center">
  <div class="col-auto">
    <% if precentage > 90 %>
      <h2 class="text-primary">Fantastisk innsats!</h2>
    <% elsif precentage > 50 %>
      <h2 class="text-primary">Bra innsats!</h2>
    <% else %>
      <h2 class="text-primary">Bra innsats, men du kan gjerne prøve igjen.</h2>
    <% end %>
  </div>
</div>
<div class="row justify-content-center">
  <% if precentage > 90 %>
    <%= image_tag "great_effort.jpg", class: "img-fluid w-25" %>
  <% elsif precentage > 50 %>
    <%= image_tag "good_effort.jpg", class: "img-fluid w-25", height: "100" %>
  <% else %>
    <%= image_tag "try_again.jpg", class: "img-fluid w-25", height: "100" %>
  <% end %>
</div>
<div class="row justify-content-center">
  <div class="col-auto">
    <h3 class="text-primary">Du fikk <%= session[:score] %> av <%= session[:total_steps] %> riktig</h3>
  </div>
</div>
<div class="row justify-content-center">
  <div class="col-auto">
    <%= link_to "Start oppgaven på nytt", new_run_path(@assignment.public_task_code), class: "btn btn-primary btn-lg" %>
  </div>
</div>
<div class="row justify-content-center">
  <div class="col-auto">
    <%= link_to "Hjem", root_path %>
  </div>
</div>
